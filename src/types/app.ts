export interface User {
      name: string;
      email: string;
      avatar: string;
}

// export another types or interface below
export interface productItems {
      id: number;
      title: string;
      price: string;
      description: string;
    }